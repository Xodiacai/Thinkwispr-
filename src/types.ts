export interface CustomWord {
  id: string;
  word: string;
}

export interface Testimonial {
  id: string;
  name: string;
  handle: string;
  role: string;
  avatar: string;
  tweet: string;
  date: string;
}
